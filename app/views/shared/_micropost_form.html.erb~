<%= form_for(@micropost) do |f| %>
	<%= render 'shared/error_messages' %>
	<div class="field">
		<%= f.text_area :content %>
	</div>
	<div class="actions">
		<%= f.submit "Submit" %>
	</div>
<% end %>